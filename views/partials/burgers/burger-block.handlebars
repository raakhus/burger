<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}EAT ME!!!{{else}}MAKE ME!!!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">THIS WAS A MISTAKE!</button>
</li>
