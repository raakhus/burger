<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}EAT NOW!{{else}}MAKE ME!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">THAT WAS A MISTAKE!</button>
</li>
