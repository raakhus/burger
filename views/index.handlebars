<h1>BURGERS!</h1>

<h2>BURGERS AVAILABLE!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make A Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgersN">Burger Name:</label>
    <input type="text" id="burgersN" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Hungary?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devour!<br>
    <input type="radio" name="devoured" value="0"> Add to Menuu
  </div>

  <button type="submit">Add Burgers</button>
</form>
